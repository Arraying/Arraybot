buildscript {
    ext.kotlin_version = "1.1.4-3"
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

plugins {
    id "java"
    id "application"
    id "com.github.johnrengelman.shadow" version "2.0.1"
}

group "de.arraying.arraybot"
version "5.0.0"

ext.main = "Arraybot"
mainClassName = "de.arraying.arraybot.Arraybot"

apply plugin: "java" // Java
apply plugin: "kotlin" // Kotlin

sourceCompatibility = 1.8

repositories {
    jcenter()
    mavenCentral()
    maven {
        url "https://jitpack.io" // Arraying's repositories
    }
}

dependencies {
    compile "com.github.Arraying:Kotys:0.3.3" // JSON
    compile "com.github.Arraying:Zeus:0.4.3" // Script engine
    compile "ch.qos.logback:logback-classic:1.2.3" // Logging
    compile "net.dv8tion:JDA:3.3.1_308" // Discord
    compile "biz.paluch.redis:lettuce:5.0.0.Beta1" // Database
    compile "commons-io:commons-io:2.5" // Utils
    compile "commons-lang:commons-lang:2.6" // Utils
    compile "org.jetbrains.kotlin:kotlin-stdlib-jre8:$kotlin_version" // Kotlin
    compile "org.jetbrains.kotlinx:kotlinx-coroutines-core:0.15" // Kotlin Coroutines
}


kotlin {
    experimental {
        coroutines 'enable'
    }
}
